# Hey Oko Fitness Website Template - Environment Variables

# =============================================================================
# PUBLIC ENVIRONMENT VARIABLES (Exposed to Browser)
# These variables are prefixed with NEXT_PUBLIC_ and will be available in client-side code
# =============================================================================

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://your-fitness-site.com
NEXT_PUBLIC_APP_NAME="Your Fitness Business"
NEXT_PUBLIC_SITE_DESCRIPTION="Professional personal training and wellness services"

# =============================================================================
# PRIVATE ENVIRONMENT VARIABLES (Server-side only)
# These variables are NOT exposed to the browser and should contain sensitive data
# =============================================================================

# Cloudinary Configuration (Image Management)
# Get these from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
# Alternative: Use Cloudinary URL (contains all above info)
# CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Resend Email Service (Contact Forms)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key
RESEND_FROM_EMAIL=noreply@your-domain.com
RESEND_FROM_NAME="Your Fitness Business"

# Contact & Booking Email Addresses
CONTACT_EMAIL=info@your-domain.com
BOOKING_EMAIL=bookings@your-domain.com

# Sanity CMS (Optional - for blog/content management)
# Get these from: https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_VERSION=2024-01-01
SANITY_TOKEN=your_sanity_token

# Calendly Integration (Optional - for booking)
# Get Personal Access Token from: https://calendly.com/integrations/api_webhooks
CALENDLY_API_TOKEN=your_personal_access_token
CALENDLY_WEBHOOK_SIGNING_KEY=your_webhook_signing_key

# =============================================================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# =============================================================================

# Development Settings
NODE_ENV=development
# Use this for local development
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================

# For production deployment, update these:
# NODE_ENV=production
# NEXT_PUBLIC_SITE_URL=https://your-actual-domain.com

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local:
#    cp .env.example .env.local

# 2. Update the values with your actual credentials

# 3. NEVER commit .env.local to version control
#    (It's already in .gitignore)

# 4. For production, set these variables in your hosting platform:
#    - Vercel: Project Settings > Environment Variables
#    - Netlify: Site Settings > Environment Variables
#    - Railway: Project > Variables

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - NEVER expose API secrets with NEXT_PUBLIC_ prefix
# - NEVER commit real API keys to version control
# - Use different keys for development and production
# - Regularly rotate API keys for security
# - Restrict API key permissions to minimum required access